// Elabore alguns códigos e imprima o resultado no console usando o console.log, um para cada operação aritmética básica. Seu código deve ter duas constantes, a e b, definidas no começo com os valores que serão operados. Escreva códigos para:
// Adição (a + b)
// Subtração (a - b)
// Multiplicação (a * b)
// Divisão (a / b)
// Módulo (a % b)

// const a = 10;
// const b = 5;

// console.log(a+b);
// console.log(a-b);
// console.log(a*b);
// console.log(a/b);
// console.log(a%b);

// Utilize if/else para escrever um código que retorne o maior de dois números. Defina, no começo do seu código, duas constantes com os valores que serão comparados.

// const a = 10;
// const b = 5;

// if (a > b) {
//   console.log(a);
// } else {
//   console.log(b);
// }

// Utilize if/else para escrever um código que retorne o maior de três números. Defina, no começo do seu código, três constantes com os valores que serão comparados.

// const c = 10;
// const d = 5;
// const e = 3;

// if (c > d || c > e) {
//   console.log("O maior número é ", c);
// } else if (d > c || d > e) {
//   console.log("O maior número é ", d);
// } else {
//   console.log("O maior número é ", e);
// }

// Utilize if/else para escrever um código que, dado um valor recebido como parâmetro, retorne “positive” se esse valor for positivo, “negative” se for negativo, e caso não seja nem positivo e nem negativo retorne “zero”.

// valor = -5;

// if (valor > 0) {
//   console.log("O valor é POSITIVO");
// } else if (valor < 0) {
//   console.log("O valor é NEGATIVO");
// } else {
//   console.log("O valor é ZERO")
// }

// // 🚀 Utilize if/else para escrever um código que defina três constantes com os valores dos três ângulos internos de um triângulo. Retorne true se os ângulos representarem os ângulos de um triângulo e false, caso contrário. Se algum ângulo for inválido, você deve retornar uma mensagem de erro.

// 1- Para os ângulos serem de um triângulo válido, a soma dos três deve ser 180 graus.
// 2- Um ângulo será considerado inválido se não tiver um valor positivo.

const anguloA = 60;
const anguloB = 70;
const anguloC = 60;

if (anguloA > 0 && anguloB > 0 && anguloC > 0 && anguloA + anguloB + anguloC == 180) {
  console.log(true);
} else if (anguloA < 0 || anguloB < 0 || anguloC < 0) {
  console.log("Erro: Ângulo Inválido. Um ângulo deve ser maior que 0")
} else {
  console.log(false);
}

// Forma de resolver pelo gabarito:
const ladoA = 65;
const ladoB = 100;
const ladoC = 15;

let somaDosLados = ladoA + ladoB + ladoC;

let todosLadosPositivos = ladoA > 0 && ladoB > 0 && ladoC > 0;

if(todosLadosPositivos){
  if (somaDosLados === 180) {
    console.log(true);
  } else {
    console.log(false);
  };
} else {
  console.log('Erro: ângulo inválido');
}

